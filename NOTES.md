# Notes

> The commands names need to be in lowercase

> interaction.options.get() => return a string | boolean | number | undefined (have to parse it)

> get() => need to be the exact as the optionName that you put

> PresenceData => ActivityOptions[] = [name:"name_activity,type:ActivityType_enum,url"] <= presence

> Object.assign => return the target object
